- name: dotfilesをclone
  git:
    repo: https://github.com/u1aryz/dotfiles.git
    dest: ~/.config/dotfiles
    depth: 1

- name: シンボリックリンクを作成
  file:
    src: "~/.config/dotfiles/{{ item.src }}"
    dest: "{{ item.dest }}"
    state: link
    force: yes
  with_items: "{{ files }}"
